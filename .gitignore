# Secrets
.env
.env.*
!.env.example
*.key
*.pem
secrets/

# Research clones (vendored upstream repos, not part of Chimera)
research/_clones/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.uv-cache/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Node / TS (future control plane)
node_modules/
dist/
.next/
*.tsbuildinfo

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Build / logs
build/
*.log

# Runtime state (keep directory via .gitkeep, ignore contents)
state/*
!state/.gitkeep

# Scenario-spawned peer Chimera state
peer_chimera/
remote_federation/

# CodeGraph local index (machine-local SQLite knowledge graph)
.codegraph/

# graphify skill output (machine-local generated knowledge graph)
graphify-out/

# WALK-generated foreign backlog specs (ADR 0186) — runtime artifacts
# regenerated idempotently from labelled issues; claimed via the crawl
# dispatch log. Operator-authored specs (NN-*.md) stay tracked.
mind/backlog/issue-*.md
