# Question q004

Per-model guardrail validation: when the same charter / prompt-injection probes are run across a cross-vendor model roster, do the guardrail failures cluster by vendor (NRT-Bench's "near-disjoint vulnerabilities"), and is a single-shot probe enough to trust the verdict?
