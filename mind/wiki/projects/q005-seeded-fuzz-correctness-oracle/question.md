# Question q005

Seeded-fuzz correctness oracle: a fixed-input test can certify buggy code (the "correctness illusion"). Can Chimera adopt seeded property/fuzz testing against a reference oracle as a code-production gate — and which oracle source actually fits the kind of code Chimera writes?
