## Test results

**no tests ran** — `tests/test_cli_escalations.py` does not exist in this worktree (checked 2025-07-16). The closest matches are `tests/test_task_escalation.py` and `tests/test_cli_trust.py`.
