# Chimera — Inbox

Active tasks. The ASSESS phase parses `- [ ]` lines into a task list each cycle;
the WRITE phase flips them to `- [x]` when the ACT phase produced matching write_targets.

Operators (humans, for now) add tasks here.

## Tasks

- [ ] (seed) Boot the agent loop end-to-end on both Anthropic and OpenRouter providers.
